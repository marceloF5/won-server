{
  "routes": [
    {
      "method": "GET",
      "path": "/publishes",
      "handler": "publish.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/publishes/count",
      "handler": "publish.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/publishes/:id",
      "handler": "publish.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/publishes",
      "handler": "publish.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/publishes/:id",
      "handler": "publish.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/publishes/:id",
      "handler": "publish.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
